name: yiclass
description: 一个课表管理工具，支持多种课程表格式导入，支持多种平台。

version: 2.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  font_awesome_flutter: ^10.7.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: any

flutter:
  uses-material-design: true

